# ─────────────────────────────────────────────────────────────
# Livestrym Scanner — requirements.txt
# IMPORTANT: No psycopg2, no sqlalchemy, no fastapi
# Scanner talks to API via HTTP only — no direct DB access
# ─────────────────────────────────────────────────────────────

# ── HTTP — calls API endpoints ────────────────────────────────
requests==2.32.3

# ── YouTube API ───────────────────────────────────────────────
google-api-python-client==2.118.0

# ── Stream download ───────────────────────────────────────────
yt-dlp>=2024.1.1

# ── Audio fingerprinting ──────────────────────────────────────
librosa==0.10.2
numpy>=1.26.0
soundfile>=0.12.1

# ── Visual fingerprinting ─────────────────────────────────────
imagehash==4.3.1
Pillow>=10.0.0

# ── Config ────────────────────────────────────────────────────
python-dotenv==1.0.1
